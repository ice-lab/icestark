{
  "name": "icestark-monorepo",
  "version": "2.7.0",
  "private": true,
  "description": "Icestark is a JavaScript library for multiple projects, Ice workbench solution.",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "setup": "rm -rf node_modules && rm -rf ./packages/*/node_modules && pnpm i",
    "build": "run-s build-icestark build-sandbox",
    "build-icestark": "cd packages/icestark && rimraf lib && npm run build",
    "build-sandbox": "cd packages/sandbox && rimraf lib && npm run build",
    "clean": "rimraf packages/*/lib",
    "install:deps": "rm -rf node_modules && rm -rf ./packages/*/node_modules && yarn install && lerna exec -- npm install",
    "publish:packages": "ts-node ./scripts/publish.ts",
    "watch": "tsc -w",
    "prepublishOnly": "npm run lint && npm run test && npm run build",
    "lint": "eslint --cache --ext .js,.jsx,.ts,.tsx ./",
    "test": "NODE_ENV=unittest jest",
    "coverage": "codecov"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/alibaba/ice/issues"
  },
  "homepage": "https://github.com/ice-lab/icestark",
  "husky": {
    "hooks": {
      "pre-commit": "tsc --noEmit && lint-staged",
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix",
      "git add"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ice-lab/icestark.git"
  },
  "devDependencies": {
    "@commitlint/cli": "^7.5.2",
    "@commitlint/config-conventional": "^7.5.0",
    "@iceworks/spec": "^1.3.2",
    "@testing-library/jest-dom": "^4.2.3",
    "@testing-library/react": "^9.3.2",
    "@types/jest": "^24.0.12",
    "@types/node": "^12.0.0",
    "codecov": "^3.4.0",
    "eslint": "^7.31.0",
    "husky": "^2.2.0",
    "jest": "^24.7.1",
    "jest-fetch-mock": "^2.1.2",
    "lint-staged": "^10.5.3",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "stylelint": "^10.1.0",
    "ts-jest": "^24.0.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.3.5"
  },
  "jest": {
    "coverageDirectory": "./coverage/",
    "collectCoverage": true,
    "preset": "ts-jest",
    "automock": false,
    "setupFiles": [
      "./setupJest.ts"
    ]
  },
  "dependencies": {
    "@types/execa": "^2.0.0",
    "@types/fs-extra": "^9.0.13",
    "execa": "^6.0.0",
    "fs-extra": "^10.0.0",
    "urllib": "^2.38.0"
  }
}
