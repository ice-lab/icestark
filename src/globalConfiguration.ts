const globalConfiguration = {
  shouldAssetsRemove: () => true,
  onRouteChange: (pathname, query, hash, type) => {},
  onAppEnter: () => {},
  onAppLeave: () => {},
  onLoadingApp: () => {},
  onError: () => {},
  onNotFound: () => {},
};

export default globalConfiguration;